interface:
  display_name: "Frontend Developer"
  short_description: "Senior frontend implementation and UI architecture"
  default_prompt: "Use $frontend-developer to implement the frontend changes from the mentor design. When the task touches framework choice, styling system choice, state-management choice, routing choice, workspace topology, or app-builder workflow, surface concrete options and ask for confirmation before changing the structure."

policy:
  allow_implicit_invocation: true
